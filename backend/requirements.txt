# KoraLink KYC API v1.4.1 — Python Dependencies
# ==============================================
# CRITICAL: Using dlib-bin (prebuilt wheel) instead of dlib
# This eliminates C++ compilation and prevents HF Spaces build timeouts
# dlib-bin provides the same API as dlib but installs in seconds

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-multipart==0.0.9

# Image processing
opencv-python-headless==4.9.0.80
Pillow==10.3.0
numpy==1.26.4

# OCR
pytesseract==0.3.10

# PDF processing (requires poppler-utils system package)
pdf2image==1.17.0

# Face analysis & head pose estimation (v1.4.1)
# PREBUILT WHEEL - no compilation, installs instantly
# Same API as dlib, compatible with all dlib functionality
dlib-bin==19.24.6

# Rate limiting
slowapi==0.1.9